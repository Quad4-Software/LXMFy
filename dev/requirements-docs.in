# Source for docs/requirements-docs.txt. Docs tooling plus lxmfy's
# runtime deps (needed to import lxmfy for the API reference) and
# mike's PyPI deps (mike itself is hash-pinned as a tarball in the txt).
zensical==0.0.63
mkdocstrings-python==2.0.8
griffe2md==1.5.0
jinja2
pyparsing>=3.0
verspec
rns==1.5.4
lxmf==1.1.1
cbor2==6.1.3
landlockpy==0.1.1 ; sys_platform == 'linux'
